<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="16dp"
    app:cardCornerRadius="16dp"
    app:cardElevation="8dp"
    android:backgroundTint="#FFFFFF">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="24dp">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Bantu AI Mengenali Makanan"
            android:textColor="@color/black"
            android:textSize="18sp"
            android:textStyle="bold"
            android:fontFamily="@font/afacad"
            android:layout_marginBottom="16dp"/>

        <!-- WADAH FOTO & TOMBOL ULANG (RETAKE) -->
        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="200dp"
            android:layout_marginBottom="16dp">

            <androidx.cardview.widget.CardView
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                app:cardCornerRadius="12dp"
                app:cardElevation="0dp">

                <ImageView
                    android:id="@+id/ivHintImage"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:scaleType="centerCrop"/>
            </androidx.cardview.widget.CardView>

            <!-- Tombol Retake (Bulat Merah Muda) -->
            <androidx.cardview.widget.CardView
                android:id="@+id/btnRetakeImage"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:layout_gravity="bottom|end"
                android:layout_margin="12dp"
                android:clickable="true"
                android:focusable="true"
                android:foreground="?attr/selectableItemBackgroundBorderless"
                app:cardCornerRadius="24dp"
                app:cardBackgroundColor="#FFB4B4"
                app:cardElevation="4dp">

                <ImageView
                    android:layout_width="24dp"
                    android:layout_height="24dp"
                    android:layout_gravity="center"
                    android:src="@android:drawable/ic_menu_revert"
                    app:tint="@color/black"/>
            </androidx.cardview.widget.CardView>
        </FrameLayout>

        <EditText
            android:id="@+id/etHint"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="24dp"
            android:backgroundTint="@color/gray_light"
            android:hint="Contoh: Rendang, Telur, dll"
            android:padding="12dp"
            android:textColor="@color/black"
            android:fontFamily="@font/afacad"
            android:textSize="14sp" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="end">

            <Button
                android:id="@+id/btnSkipHint"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Lewati"
                android:textColor="@color/black"
                android:backgroundTint="#EFEFEF"
                android:fontFamily="@font/afacad"
                android:layout_marginEnd="12dp"
                style="?android:attr/borderlessButtonStyle"/>

            <Button
                android:id="@+id/btnAnalyzeHint"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Analisis"
                android:textColor="@color/black"
                android:textStyle="bold"
                android:fontFamily="@font/afacad"
                android:backgroundTint="#D4FF6B"/>
        </LinearLayout>
    </LinearLayout>
</androidx.cardview.widget.CardView>